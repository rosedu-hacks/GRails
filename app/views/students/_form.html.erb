<%= form_for(@student) do |f| %>
  <% if @student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% @student.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <h3> Parent info </h3>
      <%= f.label :mother_name %>
      <%= f.text_field :mother_name %>

      <%= f.label :father_name %>
      <%= f.text_field :father_name %>

      <%= hidden_field_tag 'user_id', current_user.id %>

 <%= fields_for :contact  do |c| %>
    <h3> Identification </h3>
      <%= c.label :id_type %>
      <%= c.text_field :id_type %>

      <%= c.label :cnp %>
      <%= c.text_field :cnp %>

      <%= c.label :serie %>
      <%= c.text_field :serie %>

      <%= c.label :number %>
      <%= c.text_field :number %>

    <h3> Location info </h3>
      <%= c.label :country %>
      <%= c.text_field :country %>

      <%= c.label :region %>
      <%= c.text_field :region %>

      <%= c.label :city %>
      <%= c.text_field :city %>

      <%= c.label :ethnicity %>
      <%= c.text_field :ethnicity%>

      <%= c.label :citizenship %>
      <%= c.text_field :citizenship %>

      <%= c.label :address %>
      <%= c.text_field :address%>

      <%= c.label :telephone %>
      <%= c.text_field :telephone %>
 <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
